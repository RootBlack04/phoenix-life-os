"use client";
import { useRef, useTransition } from "react";
import { addIncome } from "@/lib/db/actions";
import { Card, CardHeader } from "@/components/ui/card";
export function IncomeEntryForm(){const form=useRef<HTMLFormElement>(null);const [pending,start]=useTransition();return <Card><CardHeader title="Add Income" eyebrow="Persist a new income record"/><form ref={form} className="grid grid-cols-1 md:grid-cols-2 gap-3" action={fd=>{const num=(k:string)=>{const v=fd.get(k);return v?Number(v):undefined};start(async()=>{await addIncome({source:String(fd.get("source")),amount:Number(fd.get("amount")),goal:num("goal"),type:String(fd.get("type")) as "FREELANCE"|"REMOTE_JOB"|"SAVINGS"|"OTHER",month:new Date()});form.current?.reset();});}}><input name="source" required placeholder="Source" className="field"/><input name="amount" required type="number" step="0.01" placeholder="Amount" className="field"/><input name="goal" type="number" step="0.01" placeholder="Goal" className="field"/><select name="type" defaultValue="FREELANCE" className="field"><option value="FREELANCE">Freelance</option><option value="REMOTE_JOB">Remote Job</option><option value="SAVINGS">Savings</option><option value="OTHER">Other</option></select><button disabled={pending} className="md:col-span-2 rounded-lg bg-gradient-to-r from-accent-blue to-accent-purple px-4 py-2 text-sm font-medium text-white">{pending?"Saving…":"Add income"}</button></form></Card>;}
